# AI 학습동아리 랜딩페이지 구현 설명서
> 비개발자도 따라할 수 있는 단계별 설정 가이드

---

## 목차
1. [전체 시스템 구조 이해](#1-전체-시스템-구조-이해)
2. [Google Sheets 준비](#2-google-sheets-준비)
3. [Apps Script 코드 등록](#3-apps-script-코드-등록)
4. [웹 앱 배포 (URL 발급)](#4-웹-앱-배포-url-발급)
5. [index.html 파일 설정](#5-indexhtml-파일-설정)
6. [파일 웹에 올리기 (무료 호스팅)](#6-파일-웹에-올리기-무료-호스팅)
7. [운영 방법 (관리자 가이드)](#7-운영-방법-관리자-가이드)
8. [자주 묻는 문제 해결](#8-자주-묻는-문제-해결)

---

## 1. 전체 시스템 구조 이해

```
[사용자 브라우저]
    ↕ fetch() HTTP 요청
[Apps Script 웹 앱] ← 서버 역할 (Google이 무료 제공)
    ↕ SpreadsheetApp
[Google Sheets] ← 데이터베이스 역할
```

**필요한 것:** Google 계정 1개, index.html 파일  
**비용:** 완전 무료  
**한계:** Apps Script 하루 실행 횟수 제한 있음 (소규모 동아리는 문제 없음)

---

## 2. Google Sheets 준비

### 2-1. 새 스프레드시트 만들기
1. 브라우저에서 https://sheets.google.com 접속
2. 왼쪽 상단 **＋ 새 스프레드시트** 클릭
3. 제목을 **"AI학습동아리-DB"** 로 변경 (상단 클릭해서 수정)

### 2-2. 시트 6개 만들기
스프레드시트 하단의 **＋** 버튼을 눌러 시트를 추가하고 아래와 같이 이름을 설정합니다.

| 시트 이름 | 용도 |
|----------|------|
| `members` | 회원가입 정보 저장 |
| `opinions` | 의견 게시판 |
| `vote_options` | 투표 날짜 후보 (관리자 입력) |
| `votes` | 투표 결과 |
| `news` | AI 뉴스 캐시 |
| `contests` | AI 공모전 목록 |

> **시트 이름 변경 방법:** 하단 탭을 더블클릭 → 이름 입력 → Enter

### 2-3. 각 시트 헤더 입력

**members 시트** (A1부터 입력):
```
id | name | phone | email | birthdate | agreed | joined_at
```

**opinions 시트**:
```
id | author | content | password | timestamp
```

**vote_options 시트** (관리자가 날짜 후보를 여기에 입력):
```
date | time | label | active
```
예시 데이터:
```
2026-07-05 | 14:00 | 7월 5일 오후 2시 | Y
2026-07-12 | 14:00 | 7월 12일 오후 2시 | Y
2026-07-19 | 10:00 | 7월 19일 오전 10시 | Y
```

**votes 시트**:
```
option_id | voter_name | timestamp
```

**news 시트**:
```
id | title | summary | url | source | published_at | cached_at
```

**contests 시트** (관리자가 직접 입력):
```
id | title | organizer | deadline | prize | category | url | description
```

### 2-4. 스프레드시트 ID 복사
URL에서 ID를 확인합니다:
```
https://docs.google.com/spreadsheets/d/【여기가ID】/edit
```
이 ID를 메모장에 복사해 두세요.

---

## 3. Apps Script 코드 등록

### 3-1. Apps Script 진입
1. 스프레드시트 상단 메뉴 → **확장 프로그램** → **Apps Script** 클릭
2. 새 탭에서 Apps Script 편집기가 열림
3. 기존 코드 (`function myFunction() {}`) 전체 삭제

### 3-2. 코드 붙여넣기
아래 전체 코드를 복사해서 붙여넣고, **YOUR_SPREADSHEET_ID** 부분을 2-4에서 복사한 ID로 교체합니다.

```javascript
// ══════════════════════════════════════════════
// AI 학습동아리 Apps Script 백엔드
// SHEET_ID를 반드시 본인 스프레드시트 ID로 교체하세요
// ══════════════════════════════════════════════
const SHEET_ID = 'YOUR_SPREADSHEET_ID';

function doGet(e)  { return handleRequest(e); }
function doPost(e) { return handleRequest(e); }

function handleRequest(e) {
  let action, data;
  try {
    if (e.postData && e.postData.contents) {
      data   = JSON.parse(e.postData.contents);
      action = data.action;
    } else {
      data   = e.parameter;
      action = e.parameter.action;
    }
  } catch(err) {
    data = e.parameter; action = e.parameter.action;
  }

  let result;
  try {
    switch(action) {
      case 'getOpinions':    result = getOpinions();         break;
      case 'addOpinion':     result = addOpinion(data);      break;
      case 'deleteOpinion':  result = deleteOpinion(data);   break;
      case 'getVoteOptions': result = getVoteOptions();      break;
      case 'getVotes':       result = getVotes();            break;
      case 'addVote':        result = addVote(data);         break;
      case 'register':       result = registerMember(data);  break;
      case 'getNews':        result = getNews();             break;
      case 'getContests':    result = getContests();         break;
      default:               result = { error: 'Unknown action: ' + action };
    }
  } catch(err) {
    result = { error: err.message };
  }

  return ContentService
    .createTextOutput(JSON.stringify(result))
    .setMimeType(ContentService.MimeType.JSON);
}

// ── 의견 게시판 ──────────────────────────────
function getOpinions() {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('opinions');
  const rows  = sheet.getDataRange().getValues().slice(1);
  return rows
    .filter(r => r[0])
    .map(r => ({ id: r[0], author: r[1], content: r[2], timestamp: r[4] }));
}

function addOpinion(data) {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('opinions');
  const id    = Date.now().toString();
  sheet.appendRow([id, data.author, data.content, data.password, new Date().toISOString()]);
  return { success: true, id };
}

function deleteOpinion(data) {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('opinions');
  const rows  = sheet.getDataRange().getValues();
  for (let i = 1; i < rows.length; i++) {
    if (String(rows[i][0]) === String(data.id)) {
      if (String(rows[i][3]) !== String(data.password)) {
        return { error: '비밀번호가 올바르지 않습니다.' };
      }
      sheet.deleteRow(i + 1);
      return { success: true };
    }
  }
  return { error: '게시물을 찾을 수 없습니다.' };
}

// ── 투표 ──────────────────────────────────────
function getVoteOptions() {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('vote_options');
  const rows  = sheet.getDataRange().getValues().slice(1);
  return rows
    .filter(r => r[0] && r[3] === 'Y')
    .map((r, i) => ({ id: i + 1, date: r[0], time: r[1], label: r[2] }));
}

function getVotes() {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('votes');
  const rows  = sheet.getDataRange().getValues().slice(1);
  const tally = {};
  rows.filter(r => r[0]).forEach(r => {
    const key = String(r[0]);
    if (!tally[key]) tally[key] = [];
    tally[key].push(r[1]);
  });
  return tally;
}

function addVote(data) {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('votes');
  (data.optionIds || []).forEach(id => {
    sheet.appendRow([id, data.voter, new Date().toISOString()]);
  });
  return { success: true };
}

// ── 회원가입 ──────────────────────────────────
function registerMember(data) {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('members');
  const rows  = sheet.getDataRange().getValues().slice(1);
  // 이메일 중복 체크
  const exists = rows.some(r => r[3] === data.email);
  if (exists) return { error: '이미 가입된 이메일입니다.' };

  const id = Date.now().toString();
  sheet.appendRow([id, data.name, data.phone, data.email, data.birthdate, data.agreed, new Date().toISOString()]);
  return { success: true };
}

// ── 뉴스 (캐시 방식) ─────────────────────────
function getNews() {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('news');
  const rows  = sheet.getDataRange().getValues().slice(1);
  return rows
    .filter(r => r[0])
    .slice(-10)
    .map(r => ({ id: r[0], title: r[1], summary: r[2], url: r[3], source: r[4], published_at: r[5] }))
    .reverse();
}

// ── 공모전 ────────────────────────────────────
function getContests() {
  const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('contests');
  const rows  = sheet.getDataRange().getValues().slice(1);
  return rows
    .filter(r => r[0])
    .map(r => ({ id: r[0], title: r[1], organizer: r[2], deadline: r[3], prize: r[4], category: r[5], url: r[6], description: r[7] }));
}

// ── 뉴스 자동 수집 (트리거용) ─────────────────
// Google Apps Script에서는 외부 크롤링이 제한됩니다.
// 대신 Google 뉴스 RSS를 활용합니다.
function fetchAndCacheNews() {
  try {
    const rssUrl = 'https://news.google.com/rss/search?q=생성형AI+인공지능&hl=ko&gl=KR&ceid=KR:ko';
    const response = UrlFetchApp.fetch(rssUrl);
    const xml  = response.getContentText();
    const doc  = XmlService.parse(xml);
    const root = doc.getRootElement();
    const channel = root.getChild('channel');
    const items = channel.getChildren('item').slice(0, 5);

    const sheet = SpreadsheetApp.openById(SHEET_ID).getSheetByName('news');
    // 기존 데이터 전체 삭제 (헤더 제외)
    const lastRow = sheet.getLastRow();
    if (lastRow > 1) sheet.deleteRows(2, lastRow - 1);

    items.forEach((item, i) => {
      const title  = item.getChildText('title') || '';
      const link   = item.getChildText('link')  || '';
      const pubDate = item.getChildText('pubDate') || '';
      const source = item.getChild('source') ? item.getChild('source').getText() : 'Google 뉴스';
      sheet.appendRow([
        (i + 1).toString(),
        title.replace(/<[^>]+>/g, '').replace(/\s*-\s*[^-]+$/, ''),
        '',
        link,
        source,
        pubDate,
        new Date().toISOString()
      ]);
    });
    Logger.log('뉴스 업데이트 완료: ' + items.length + '개');
  } catch(err) {
    Logger.log('뉴스 수집 오류: ' + err.message);
  }
}
```

### 3-3. 저장
- **Ctrl+S** (Mac: Cmd+S) 또는 상단 💾 아이콘 클릭
- 프로젝트 이름을 **"AI학습동아리-API"** 로 변경

### 3-4. 뉴스 자동 수집 트리거 설정 (매일 오전 7시)
1. 왼쪽 메뉴에서 **⏰ 트리거** (시계 모양) 클릭
2. 오른쪽 하단 **+ 트리거 추가** 클릭
3. 아래와 같이 설정:

| 항목 | 설정값 |
|------|--------|
| 실행할 함수 | `fetchAndCacheNews` |
| 이벤트 소스 | 시간 기반 |
| 시간 기반 유형 | 일별 타이머 |
| 시간 | 오전 6시~7시 |

4. **저장** 클릭 → Google 계정 권한 허용

---

## 4. 웹 앱 배포 (URL 발급)

1. Apps Script 편집기 오른쪽 상단 **배포** → **새 배포** 클릭
2. 왼쪽 ⚙ 아이콘 → **웹 앱** 선택
3. 설정값 입력:

| 항목 | 설정값 |
|------|--------|
| 설명 | v1 |
| 다음 사용자로 실행 | **나** |
| 액세스 권한 | **모든 사용자** |

4. **배포** 클릭 → Google 계정 권한 허용
5. **웹 앱 URL** 복사 (이게 API_URL입니다!)

> ⚠ URL 형식: `https://script.google.com/macros/s/AKfyc.../exec`

---

## 5. index.html 파일 설정

1. 제공된 `index.html` 파일을 텍스트 편집기로 열기
   - Windows: 메모장 / VS Code 권장
   - Mac: TextEdit (일반 텍스트 모드) / VS Code 권장
2. 아래 줄을 찾아서:
   ```javascript
   const API_URL = 'YOUR_APPS_SCRIPT_URL_HERE';
   ```
3. 4단계에서 복사한 URL로 교체:
   ```javascript
   const API_URL = 'https://script.google.com/macros/s/AKfyc.../exec';
   ```
4. 저장 (Ctrl+S)

---

## 6. 파일 웹에 올리기 (무료 호스팅)

### 방법 A — GitHub Pages (추천, 완전 무료)
1. https://github.com 가입
2. **New repository** 클릭 → 이름: `ai-study-club`
3. **Add file** → **Upload files** → `index.html` 드래그
4. **Commit changes** 클릭
5. **Settings** → **Pages** → Source: **main** branch → **Save**
6. 약 1분 후 `https://사용자명.github.io/ai-study-club` 으로 접속 가능

### 방법 B — Netlify (드래그앤드롭, 가장 쉬움)
1. https://netlify.com 가입
2. 대시보드에서 `index.html` 파일 드래그앤드롭
3. 즉시 URL 발급 (예: `https://랜덤이름.netlify.app`)

---

## 7. 운영 방법 (관리자 가이드)

### 투표 날짜 추가/변경
`vote_options` 시트에서 직접 수정합니다.
- `active` 열에 **Y** = 투표에 표시 / **N** = 숨김

### 공모전 등록
`contests` 시트에 행을 추가합니다.
```
id(숫자) | 공모전명 | 주최기관 | 마감일(YYYY-MM-DD) | 시상금 | 카테고리 | URL | 설명
```

### 회원 목록 확인
`members` 시트에서 실시간 확인 가능합니다.

### 뉴스 수동 업데이트
Apps Script 편집기에서 `fetchAndCacheNews` 함수 선택 후 ▶ 실행 클릭.

### 코드 수정 후 재배포 방법
**배포** → **배포 관리** → 연필 아이콘 → 버전: **새 버전** → **배포**

---

## 8. 자주 묻는 문제 해결

| 증상 | 원인 | 해결 방법 |
|------|------|-----------|
| 화면에 데이터가 안 뜸 | API_URL 미설정 | index.html에서 URL 확인 |
| `CORS 오류` | 배포 액세스 권한 문제 | 4단계에서 "모든 사용자"로 재배포 |
| `Unknown action` | 함수명 오타 | Apps Script 코드 재확인 |
| 뉴스가 안 뜸 | 트리거 미설정 또는 RSS 오류 | 3-4단계 트리거 재설정 |
| 회원가입 안 됨 | 시트 이름 오타 | Sheets에서 시트명 정확히 확인 |
| 수정 후 반영 안 됨 | 재배포 누락 | 반드시 "새 버전"으로 재배포 |

---

> 📧 기술 문의가 있으면 정보화팀 지현석 리더에게 연락하세요.
